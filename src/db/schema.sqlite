PRAGMA foreign_keys = ON;

DROP TABLE IF EXISTS T_STRUCTURE_UNITS;
CREATE TABLE T_STRUCTURE_UNITS (
    id INTEGER PRIMARY KEY,
    parentId INTEGER NOT NULL,
    title TEXT,
    description TEXT,
    view TEXT NOT NULL,
    FOREIGN KEY(parentId) REFERENCES T_STRUCTURE_UNITS(id) ON DELETE RESTRICT ON UPDATE RESTRICT
);
